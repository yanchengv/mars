<nav class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">BALAWO.COM * 扒拉我</a>
    </div>

    <div id="navbar"  class="collapse navbar-collapse navbar-right">
      <form action="/movies/search" class="navbar-form navbar-left">
        <input type="text" name="name" class="form-control" placeholder="扒拉一下...">
      </form>
      <ul class="nav navbar-nav">
        <% @movie_menus.each do |movie_menu| %>
            <% if movie_menu.name == @selected_movie_menu %>
                <li class="nav-item active">
                  <a href="<%= movie_menu.url%>"><%= movie_menu.name %>
                  </a>
                </li>
            <% else %>
                <li class="nav-item">
                <a href="<%= movie_menu.url%>" class="active"><%= movie_menu.name %></a>
                </li>
            <% end %>

        <% end %>

        <% if current_user %>
            <li>
              <div class="btn-group home-login-menu">
                <button type="button" class="btn  btn-primary dropdown-toggle" data-toggle="dropdown">
                  欢迎您<%= current_user.name%> <span class="caret"></span>
                </button>
                <ul class="dropdown-menu" role="menu">
                  <li><a class="nav-link" href="/log_out">退出</a></li>
                </ul>
              </div>
            </li>
        <% else %>
            <li><a class="nav-link" href="/login">登陆</a></li>
            <li><a class="nav-link" href="/sign_up">注册</a></li>
        <% end %>

      </ul>
    </div>






  </div>
</nav>



